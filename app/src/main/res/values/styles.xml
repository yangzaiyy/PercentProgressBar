<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <declare-styleable name="HBLevelView">
        <attr name="startProgress" format="float|reference" />
        <attr name="scurrProgress" format="float|reference" />
        <attr name="endProgress" format="float|reference" />
        <attr name="textSize" format="reference|dimension" />
        <attr name="backColor" format="color|reference" />
        <attr name="foreColor" format="color|reference" />
        <attr name="textColor" format="color|reference" />
        <attr name="rectCorn" format="dimension|reference" />
    </declare-styleable>

    <declare-styleable name="ProgressBarView">
        <attr name="currProgress" format="integer|reference" />
        <attr name="progressTextSize" format="reference|dimension" />
        <attr name="progressMarginText" format="integer|dimension" />
        <attr name="progressHeight" format="integer|dimension" />
        <attr name="firstColor" format="color|reference" />
        <attr name="secondColor" format="color|reference" />
        <attr name="progressTextColor" format="color|reference" />
    </declare-styleable>

</resources>
